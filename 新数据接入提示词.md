# 新数据接入提示词（给 Trae）

> 生成日期：2026-08-19
> 背景：FloodIMG / RescueNet / STURM-Flood 三套新数据已落盘到 H 盘，需接入基础设施灾损识别程序（低洼积水/洪水方向）。Sen1Floods11 下载失败，本次不接入。

## 一、任务概述

在 `H:\实习\基础设施灾损识别` 现有代码结构（models/{water_level,road_bridge} + tools/）基础上，**新增"洪水/低洼积水检测"模型模块**，把新数据做成可训练、可推理的完整管线，并接入统一推理接口 `tools/infer_api.py`。

新增任务类型名：`flood`（洪水检测）与 `flood_seg`（洪水分割）——如无特殊理由不要改，保证 infer_api 可统一调度。

## 二、新数据位置与格式（已核验）

| 数据集 | 路径 | 数量 | 格式 | 状态 |
|---|---|---|---|---|
| FloodIMG | `H:\dev\disaster-data\image_datasets\floodimg` | 图像 ~9296 张 + 标注 25 个 JSON | labelme JSON 多边形标注（标注类别含 Truck/Flood 等，**需先统计全部类别**） | ✅ 完整 |
| RescueNet | `H:\dev\disaster-data\image_datasets\rescuenet\segmentation-trainset` | train-org-img 2941 张 jpg + train-label-img 3595 张 png | 无人机洪水分割，原图 `10778.jpg` ↔ 标签 `10778_lab.png` 同名对应 | ⏳ 仅 trainset；**注意 org/label 数量不一致（2941 vs 3595），先核对对应关系再使用** |
| STURM-Flood | `H:\dev\disaster-data\image_datasets\sturm_flood\Dataset` | Sentinel1/S1 21602 瓦片 + Floodmaps 21602（完整）；Sentinel2/S2 仅 5350（部分） | 遥感 GeoTIFF 洪水制图（EMS* 命名，1=洪水 0=非洪水 掩膜） | ⏳ S1 完整，S2 不完整 |
| Sen1Floods11 | 未落盘 | — | — | ❌ 失败，跳过 |

## 三、要做的事（按顺序）

### 1. 数据核验与统计（先做，别跳过）
- 写一个只读统计脚本（放 `tools/` 下，比如 `tools/audit_new_datasets.py`）：
  - FloodIMG：统计 JSON 标注中全部 `label` 类别及每类数量、图像与标注对应关系（标注只有 25 个 JSON？核对是否只覆盖部分图像，还是 JSON 内含多个标注文件）
  - RescueNet：核对 train-org-img 与 train-label-img 的文件名对应率（2941 vs 3595 差异原因），统计 PNG 标签类别
  - STURM：确认 S1 瓦片与 Floodmaps 命名对应、值分布（0/1）
- 输出统计报告到控制台，不要改数据文件

### 2. 数据准备（转换脚本）
- FloodIMG：labelme JSON → YOLO 检测格式（txt），新建 `tools/prepare_floodimg.py`，输出到 `H:\dev\disaster-data\image_datasets\floodimg\yolo_labels\`（沿用 road_bridge 的 `YOLO_LABELS_DIR` 模式，数据目录只读不修改，标签另写）
- RescueNet：原图 jpg + 分割 png → YOLO 分割格式（seg txt），新建 `tools/prepare_rescuenet.py`，输出到 `rescuenet\yolo_seg_labels\`
- STURM：本次只做"数据说明 + 抽样可视化验证"，**不转 YOLO、不训练**（遥感域差异大，优先级低），把结论写进训练计划

### 3. 新增模型模块（模仿现有结构）
新建 `models/flood_detection/`，包含：
- `config.py`：`DATA_DIR`、`IMAGES_DIR`、`YOLO_LABELS_DIR`、`MODEL_WEIGHTS_DIR`（存 `H:\dev\disaster-data\models\infra\flood_detection\`，不进 git）、`CLASSES`（由第 1 步统计结果定）、`CONFIDENCE_THRESHOLD`
- `inference.py`：`FloodDetector` 类，接口对齐 `models/road_bridge/inference.py` 的 `RoadDamageDetector`（有 `detect(image_path)` 返回结构化的结果 dict/列表），内部用 YOLOv8n 预训练权重（模型统一从 `H:\实习\yolov8n-seg.pt` 或 ultralytics 缓存加载）

### 4. 接入统一推理接口
- `tools/infer_api.py`：新增 `task="flood"`（检测）和 `task="flood_seg"`（分割）分支，惰性加载 `FloodDetector`
- 保持现有 `water_level`、`road` 分支行为不变，跑 `tools/self_test.py` 确认无回归

### 5. 训练（按优先级，先验收后训练）
- **先验收**：数据统计通过、转换脚本跑通、样本数达标（图像 ≥500 张）
- **训练优先级**（沿用训练计划决策表，本次只训 FloodIMG 检测）：
  1. FloodIMG 检测：⭐⭐ 中，交付侧重"图像识别"才训——**本次重点**
  2. RescueNet 分割：⭐⭐ 中，⚠️ CC BY-NC-ND 非商用许可，训练前再确认用途
  3. STURM：⭐ 低，不训
- 训练命令沿用 `python tools/train.py --model flood_detection --epochs 100`（train.py 需新增对应分支）
- **断点续训（必须支持）**：
  - ultralytics 训练每 epoch 自动保存 `weights/last.pt`，中断后可从此恢复，已完成的 epoch 不重跑
  - 给 `tools/train.py` 增加 `--resume` 参数：`python tools/train.py --model flood_detection --resume` 时用 `YOLO(project/name/weights/last.pt).train(resume=True)` 续训；`--epochs` 在续训场景表示"剩余 epoch 数"时按 ultralytics 语义处理（不指定则继续到原总 epoch）
  - 三个模型（water_level / road_bridge / flood_detection）统一支持该参数；`--resume` 无 last.pt 时报错提示而非静默从零开始
  - 数据准备（转换脚本）中断可安全重跑（幂等，重复写 YOLO txt 覆盖即可）

## 四、硬性约束

1. **所有数据只读引用 H 盘**，转换产物写 `H:\dev\disaster-data\image_datasets\*\yolo*_labels\`，权重写 `H:\dev\disaster-data\models\infra\`——不要往 C/D 盘写数据，不要在项目目录存权重
2. Python 解释器：`D:\ana\envs\disasterlex\python.exe`
3. 推理耗时目标 ≤1 秒/张（CPU）
4. RescueNet 许可为 CC BY-NC-ND，任何交付物中注明许可来源
5. 完成第 1~4 步后，把结论更新到 `H:\实习\基础设施灾损识别\训练计划.md` 和总账本 `H:\实习\项目进展与改动记录.md`，不要私自更改总账本格式

## 五、验收标准

- `tools/audit_new_datasets.py` 输出完整统计（类别/数量/对应率）
- 两个转换脚本跑通，产出 YOLO 标签文件
- `process_infra_image("洪灾现场.jpg", task="flood")` 返回检测结果，耗时 ≤1 秒
- `tools/self_test.py` 全部通过（含原有 water_level/road 无回归）
- 训练计划已更新
