# 基础设施灾损识别 — 训练计划与优先级

> 项目：`H:\实习\基础设施灾损识别`（独立于灾害链推理引擎 v2）
> 更新：2026-08-26（第六版，水位人工标注 GPU 重训完成）

## 一、训练优先级决策表

| 任务 | 数据集 | 状态 | 优先级 | 说明 |
|---|---|---|---|---|
| 道路损毁微调 | RDD2020（26336 张） | ✅ 训练完成 | **必做（已完成）** | 30 epoch, imgsz=320, best mAP50=0.269 |
| 水位尺微调 | 水位尺（167 张人工标注） | ✅ 人工标注 GPU 重训完成 | **必做（已完成）** | 150 epoch, GPU 13min, best mAP50=0.966 |
| 洪水分割 | v2 项目 flood.pt（FloodNet 训练） | ✅ 已接入 v2 分割模型 | ⭐⭐ 中 | 直接复用 v2 已训好的分割模型，输出积水面积+淹没占比+灾情等级 |
| 无人机洪水分割 | RescueNet（2941 对） | ✅ 已转换 YOLO seg | ⭐⭐ 中 | CC BY-NC-ND 非商用许可；9 类分割，训练前需确认用途 |
| 洪水制图 | STURM-Flood（21602 瓦片） | ✅ 已落盘 | ⭐ 低 | 遥感域差异大，S1 完整 S2 不完整，本次不训练 |
| SAR 洪水分割 | Sen1Floods11 | ❌ 下载失败 | ⭐ 低 | googleapis 下载中断，跳过 |
| FloodIMG 检测 | FloodIMG（9295 张，仅 25 标注） | ❌ 不可用 | ❌ 放弃 | 标注无 Flood 类，10 类通用物体，仅 25 张标注，不作为默认推理路径 |

## 二、当前进度（2026-08-26 第六版）

### ✅ 已完成
- POC 完成：代码结构 20+/20+、道路/水位/洪水分割三管线跑通
- 数据审计：FloodIMG（9295 张/10 类/25 标注，无 Flood 类）、RescueNet（2941 对/9 类）、STURM（S1 21602 对完整）
- 数据准备：FloodIMG 25 张 labelme->YOLO（103 个标注）、RescueNet 2941 张->YOLO seg（14947 个多边形）
- 模型模块：models/flood_detection/ 已完成洪水分割重构
- 推理接口：tools/infer_api.py 中 flood/flood_seg/flood_detection 三个任务名均走分割
- 训练入口：tools/train.py 保留 flood_detection 分支 + three-model --resume 断点续训
- 自测：33/33 通过（含原 water_level/road 无回归）
- **水位尺数字框弱监督标注**：EasyOCR 全量扫描 500 训练 + 205 验证图，产出 398+187 个 YOLO 标签，检出率 83.3%，抽检 30 张可视化确认可用
- **YAML 路径修复**：VocToYoloConverter 输出改为 images 同级 labels/ 目录，对齐 YOLO 标准结构
- **水位尺数字检测微调完成（弱监督 CPU 版）**：YOLOv8n, 100 epoch, CPU 训练，best mAP50=0.744, best.pt=6.2MB
- **道路损毁微调完成**：YOLOv8n, 30 epoch, imgsz=320, CPU 训练，最终 best mAP50=0.269, best.pt=6.2MB
- **水位尺人工标注 GPU 重训完成**：labelme JSON→YOLO 转换（167 张/846 框，8:2 划分），RTX 4060 训练 150 epoch/13min，best mAP50=0.966，best.pt=6.2MB

### 训练结果详情

| 任务 | 模型 | 参数 | mAP50 | mAP50-95 | best.pt | 耗时 |
|---|---|---|---|---|---|---|
| 水位尺数字检测（弱监督） | YOLOv8n | 100 epoch, 640x640, CPU | **0.744** | 0.648 | 6.2MB | ~7h |
| 水位尺数字检测（人工标注） | YOLOv8n | 150 epoch, 640x640, GPU RTX 4060 | **0.966** | 0.376 | 6.2MB | 13min |
| 道路损毁检测 | YOLOv8n | 30 epoch, 320x320, batch=32, CPU | **0.269** | 0.104 | 6.2MB | ~17h（含中断续训） |

**道路损毁各类别 mAP50：** D00=0.360, D10=0.249, D20=0.336, D40=0.133

### 权重路径
- 道路：`H:\dev\disaster-data\models\infra\road_bridge\rdd2020_yolov8n\weights\best.pt`
- 水位（人工标注 GPU 版，推理默认路径）：`H:\dev\disaster-data\models\infra\water_level\water_level_digit_detector.pt`
- 水位（弱监督 CPU 版）：`H:\dev\disaster-data\models\infra\water_level\water_level_digit\weights\best.pt`

### 验证结果
- **self_test.py**: 33/33 通过，无回归
- **水位推理验证（新版公式）**：
  - 公式：最小可见数字 N → 水位 = 1.0 + N×0.1 米 + 插值小数位
  - 100 张随机图评估：**MAE = 9.67 cm**（97 张有真值），≤ 10cm 阈值，公式可用
  - 水位输出在 1.5~1.9 米合理范围（不再出现负数/2.7cm 等异常值）
  - 数字检测每图 3-5 个数字，置信度 0.99+，OCR 放大 6 倍后识别率提升
- **建议**：若后续需进一步降低 MAE，可用全部 2691 条真值做回归估计基准偏移

## 三、已用技术栈
- YOLOv8n-seg（ultralytics）+ EasyOCR + VOC->YOLO / labelme->YOLO 转换
- 洪水分割权重：H:\dev\disaster-data\models\flood.pt（26MB，FloodNet 训练）
- 权重保存：H:\dev\disaster-data\models\infra\（不进 git）
- 环境：D:\ana\envs\disasterlex\python.exe

## 四、数据来源与许可
| 数据集 | 许可 | 备注 |
|---|---|---|
| FloodIMG | 未明确标注 | 洪水场景图像，仅 25 张有标注，无 Flood 类 |
| RescueNet | CC BY-NC-ND | 非商用许可，训练前需确认用途 |
| STURM-Flood | 未明确标注 | 遥感 Copernicus 数据，遵循 EU 法规 |